// FlameLang Example: Hebrew Operators Only
// Pure Hebrew syntax for quantum operations

דחה  // Bounce (push away)
כבש  // Suppress (subdue)
ראה  // Observe (see)
נוע  // Fluctuate (move)
אחד  // Unify (one)
פלא  // Anomaly (wonder)
