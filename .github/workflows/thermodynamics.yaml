name: thermodynamics

on:
  pull_request:
  push:

jobs:
  entropy_monitor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Build
        run: cargo build --locked

      - name: Entropy gate (placeholder)
        run: |
          echo "TODO: enforce cyclomatic complexity thresholds (Q4)."
          echo "Suggestion: integrate a Rust lint step + custom script."
